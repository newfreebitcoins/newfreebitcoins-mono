{
  "name": "new-free-bitcoins",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --ui=tui",
    "dev:backend": "npm run dev --workspace @new-free-bitcoins/backend",
    "dev:backend:cache": "npm run dev:cache --workspace @new-free-bitcoins/backend",
    "start:backend": "npm run build --workspace @new-free-bitcoins/backend && node apps/backend/dist/index.js",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
